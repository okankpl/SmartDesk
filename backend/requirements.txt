fastapi>=0.115
uvicorn[standard]>=0.32
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pydantic-settings>=2.5
bcrypt>=4.2
pyjwt>=2.9
# Wird von FastAPIs OAuth2PasswordRequestForm gebraucht (Login-Formular-Daten,
# nicht JSON), nicht direkt in unserem Code importiert.
python-multipart>=0.0.12

# Nur zum Testen benoetigt, laeuft nie im echten Betrieb mit
pytest>=8.0
